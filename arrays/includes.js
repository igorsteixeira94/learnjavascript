/**
 * Função: Verifica se um elemento existe em um array !
 * Retorno: True ou False
 * Exemplo: Verificar se o nome Igor, existe no array.
 */

const names = ['Maria', 'Carlos', 'Joao', 'Igor']
const name = 'Igor'

console.log(names.includes(name)) //Retorno: True
